"""Assessor-агент. Глава 03. Только предлагает сумму — НИКОГДА не платит сам."""


def run(handoff: dict) -> dict:
    """Предложить recommended_payout + факты-обоснование. Верни payload Assessment."""
    raise NotImplementedError("глава 03: Assessment only — never execute payout")
