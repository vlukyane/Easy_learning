"""Генератор синтетических инвойсов. Глава 02.

Синтетика даёт идеальную разметку (gold-поля известны) и достаточный объём (~40k)
для честного шанса LoRA. Варьируй шаблоны перевозчиков — иначе модель выучит один формат.
"""


def main(n: int = 2000) -> None:
    """Сгенерировать n инвойсов: документ + gold-поля (InvoiceFields). Глава 02.

    Варьируй шаблоны carrier; сохрани документы и золото, чтобы потом бить на split.
    """
    raise NotImplementedError("глава 02: документы + gold-поля; варьируй шаблоны перевозчиков")
