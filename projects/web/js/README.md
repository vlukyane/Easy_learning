# JavaScript — интерактивная доска

Портал: http://localhost:4321/tracks/web/js

Сначала глава «Стенд Node и nvm», затем `board.html` + `board.js` (`type="module"`).

```bash
cd projects/web/js
nvm use
open board.html
```
