# HTML — визитка и каркас доски

Портал: http://localhost:4321/tracks/web/html

Файлы появятся в главах 3–7: `index.html`, при желании `board.html`, картинка рядом.

```bash
cd projects/web/html
open index.html
```

Node здесь не нужен.
