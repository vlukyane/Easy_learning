# Doorman — Guardrails

Портал: http://localhost:4321/courses/doorman

Учебный стенд. Корпус атак — фикстуры для ASR. Нет инструментов почты и HTTP.

```bash
cd projects/doorman
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e ".[dev]"
cp .env.example .env
```
