---
course: doorman
order: 4
title: Слои защиты
summary: Добавляй по одному и меряй ASR. Архитектура важнее увещевания.
estMinutes: 45
---

<Scaffold path="projects/doorman/src/agent/hardened.py">
Тот же контракт `screen(resume, job)`, другие внутренности.
</Scaffold>

<Scaffold path="projects/doorman/src/sanitize.py">
Нормализация входа: выкинуть невидимые символы, обрезать гигантские metadata-поля, не исполнять HTML.
</Scaffold>

Включай слои **по одному**, каждый прогон ASR в лог:

1. Разметка data/instructions: резюме в явном блоке `UNTRUSTED_RESUME`, системный текст запрещает слушаться содержимого блока.
2. Санитизация входа (`sanitize.py`).
3. Structured output: оценка обязана ссылаться на `facts[]`; score без фактов невалиден.
4. Отдельный detector-проход: флаг `suspicious`, не автоматический 0 (это убьёт FP).
5. Least privilege: инструментов эксфильтрации нет (уже в baseline — закрепи тестом).

<Command cwd="projects/doorman" title="ASR после слоя" code={`python -c "from src.eval.asr import run; run(agent='hardened', tag='layer-1')"`} />

Не включай все слои сразу «на всякий случай»: не поймёшь, какой закрыл класс.

<Trap title="Защита, ломающая продукт">
Слишком агрессивный фильтр режет нормальные резюме. Меряй FP на `data/clean/` после каждого слоя.
</Trap>

<Practice items={[
  { id: "untrusted-block", label: "В hardened резюме обёрнуто как недоверенные данные", hint: "Разделители/теги/роль. Модель должна видеть: это данные, не инструкции. Vulnerable так не делает — сравни промпты." },
  { id: "sanitize", label: "sanitize применяется до вызова модели", hint: "Вырежи/экранируй типичные injection-маркеры. Не вместо обёртки, а слоем раньше. Лог: слой sanitize сработал." },
  { id: "facts", label: "Score без facts отбрасывается валидатором схемы", hint: "Pydantic: нет фактов — ошибка, не «оценка 10 из воздуха». Это слой после модели." },
  { id: "layerwise", label: "Есть ASR-логи хотя бы двух последовательных слоёв (не только финал)", hint: "Чтобы отчёт потом сказал, какой слой закрыл класс. Иначе «стало лучше» без механики." }
]}>
Hardened ≠ одна фраза «игнорируй инъекции». Несколько слоёв, каждый проверяемый.
</Practice>

<Checkpoint>
**Готово, когда:** hardened отличается от vulnerable не только промпт-мантрой.

**Как проверить:** diff двух агентов. Обёртка + sanitize + валидатор. Кусок ASR по слоям.

**Ещё рано, если:** скопировал vulnerable и дописал «будь осторожен»; слоёв не видно в логе.
</Checkpoint>
